[project]
name = "event-normalizer-demo"
version = "0.1.0"
description = "Webhook event normalization fixture for multi-agent evaluation"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
